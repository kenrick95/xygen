// @ts-check

(function () {
    function main () {
        const canvas = document.getElementById('canvas');
        const context = canvas.getContext('2d');
    
        // get input
        // transform to baudot code
        // draw x&y
    }
    
    document.addEventListener('DOMContentLoaded', main);F
})();
